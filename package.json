{
    "scripts": {
        "dev": "vitepress dev",
        "watch": "vitepress dev",
        "build": "vitepress build",
        "preview": "vitepress preview",
        "production": "vitepress build && vitepress preview"
    },
    "devDependencies": {
        "autoprefixer": "^10.0",
        "tailwindcss": "^3.0",
        "vitepress": "^1.0.0-rc.40"
    },
    "postcss": {
        "plugins": {
            "autoprefixer": {},
            "tailwindcss": {
                "config": ".vitepress/theme/tailwind.js"
            }
        }
    }
}